%h1 Listing offerings

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @offerings.each do |offering|
      %tr
        %td= link_to 'Show', offering
        %td= link_to 'Edit', edit_offering_path(offering)
        %td= link_to 'Destroy', offering, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Offering', new_offering_path
